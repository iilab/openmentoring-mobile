<Page xmlns="http://www.nativescript.org/tns.xsd" loaded="pageLoaded">
  <Page.actionBar>
    <ActionBar title="Open Mentoring">
      <ActionBar.actionItems>
        <ActionItem text="Settings"  ios.position="right"/>
      </ActionBar.actionItems>
    </ActionBar>
  </Page.actionBar>
  <ListView items="{{ topicItems }}" itemTap="listViewItemTap">
    <ListView.itemTemplate>
       <Label text="{{ title }}" textWrap="true" class="topic-label" />
    </ListView.itemTemplate>
 </ListView>
</Page>
